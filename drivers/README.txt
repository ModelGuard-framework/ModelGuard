OFFLINE SELENIUM SETUP

Place your locally installed ChromeDriver executable here as:
  chromedriver.exe

Or set the CHROMEDRIVER_PATH environment variable to its local path.

ModelGuard deliberately does not use Selenium Manager/download a driver because the project is required to work without internet.
